# Public Assets Folder

Place your tree.png image in this folder.

The image should be named `tree.png` and will be automatically loaded by the game.

In Vite, files in the `public/` folder are served at the root URL, so `/tree.png` will work.

